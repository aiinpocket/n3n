apiVersion: v1
kind: Service
metadata:
  name: {{ include "n3n.fullname" . }}
  labels:
    {{- include "n3n.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "n3n.selectorLabels" . | nindent 4 }}
